services:
  - type: web
    name: eisenhower-matrix-api
    rootDir: backend
    env: docker
    dockerfilePath: ./backend/Dockerfile
    envVars:
      - key: DB_URL
        sync: false
      - key: DB_USERNAME
        sync: false
      - key: DB_PASSWORD
        sync: false
    healthCheckPath: /api/tasks
